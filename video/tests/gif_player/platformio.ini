[env:pico2]
platform = raspberrypi
board = pico
framework = arduino
board_build.core = earlephilhower
board_build.filesystem_size = 1m

monitor_speed = 115200

lib_deps =
    bitbank2/AnimatedGIF @ ^2.1.1

; This is to make AnimatedGIF wotk in Arduino mode
build_flags = -UPICO_BUILD